[project]
name = "if-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Estevan", email = "estevan.mensan@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.141.1",
]

[project.scripts]
if-api = "if_api:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
